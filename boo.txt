xpress = require('express');
var router = express.Router();

/* GET users listing. */
router.get('/', function(req, res, next) {
	res.render('user', { title: 'Express',data:'respond with a resource'});
});

module.exports = router;xpress = require('express');
var router = express.Router();

/* GET users listing. */
router.get('/', function(req, res, next) {
	res.render('user', { title: 'Express',data:'respond with a resource'});
});

module.exports = router;xpress = require('express');
var router = express.Router();

/* GET users listing. */
router.get('/', function(req, res, next) {
	res.render('user', { title: 'Express',data:'respond with a resource'});
});

module.exports = router;xpress = require('express');
var router = express.Router();

/* GET users listing. */
router.get('/', function(req, res, next) {
	res.render('user', { title: 'Express',data:'respond with a resource'});
});

module.exports = router;xpress = require('express');
var router = express.Router();

/* GET users listing. */
router.get('/', function(req, res, next) {
	res.render('user', { title: 'Express',data:'respond with a resource'});
});

module.exports = router;xpress = require('express');
var router = express.Router();

/* GET users listing. */
router.get('/', function(req, res, next) {
	res.render('user', { title: 'Express',data:'respond with a resource'});
});

module.exports = router;
Rohan Ranjan